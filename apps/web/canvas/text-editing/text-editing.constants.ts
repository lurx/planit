/** Line-height multiple for the editor, sizing the single-line box around the font. */
export const TEXT_LINE_HEIGHT = 1.2;

/** Screen-space slop (px) when hit-testing the double-clicked shape; divided by zoom. */
export const TEXT_HIT_TOLERANCE_PX = 6;
