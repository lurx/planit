/** Exponential zoom response to wheel delta. Higher = faster zoom per wheel notch. */
export const ZOOM_WHEEL_SENSITIVITY = 0.002;

/** Multiplicative zoom step for the toolbar +/- buttons (T4.1). */
export const ZOOM_BUTTON_STEP = 1.2;
