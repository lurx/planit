/**
 * @planit/shared — the keystone package.
 *
 * Domain types, the CRDT document schema, validation, and the wire protocol live here so
 * that `apps/web` and (later) `apps/ws` import byte-identical definitions. Populated
 * across Phase 1 (domain model, geometry, camera) onward.
 */

export const SHARED_PACKAGE_NAME = '@planit/shared';
